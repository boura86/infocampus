{% extends 'base.html.twig' %}

{% block body %}
    <a  class="btn btn-default" href="{{ path('facultes_index') }}">Liste</a>
    <h1 class="page-header">Détails d'une faculté</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ faculte.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ faculte.nom }}</td>
            </tr>
        </tbody>
    </table>

            <a class="btn btn-success col-md-1"  href="{{ path('facultes_edit', { 'id': faculte.id }) }}">Edit</a>

            {{ form_start(delete_form) }}
                <input class="btn  btn-danger col-lg-offset-2 col-md-1" type="submit" value="Delete">
            {{ form_end(delete_form) }}
{% endblock %}
