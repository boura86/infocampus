{% extends 'base.html.twig' %}

{% block body %}
    <a class="btn btn-default" href="{{ path('structures_index') }}">Liste</a>
    <h1 class="page-header">Modifier une structure</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn btn-success" style="margin-top: 15px" type="submit" value="Edit" />
    {{ form_end(edit_form) }}


    {#{{ form_start(delete_form) }}#}
        {#<input class="btn btn-danger" style="margin-top: 15px" type="submit" value="Delete">#}
    {#{{ form_end(delete_form) }}#}

{% endblock %}
