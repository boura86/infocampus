{% extends 'base.html.twig' %}

{% block body %}
    <a class="btn btn-default" href="{{ path('structures_index') }}">Back to the list</a>
    <h1 class="page-header">Détails d'une structure</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ structure.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ structure.nom }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row">

        <a class="btn btn-success col-md-1" href="{{ path('structures_edit', { 'id': structure.id }) }}">Edit</a>

        {{ form_start(delete_form) }}
        <input class="btn btn-danger col-lg-offset-2 col-md-1" type="submit" value="Delete">
        {{ form_end(delete_form) }}
    </div>

{% endblock %}
